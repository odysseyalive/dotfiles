#!/usr/bin/env bash
# This script is just a personal snapshot of my arch workstation.
# curl https://raw.githubusercontent.com/bridgesense/dotfiles/master/workstation/endeavouros-gemini > ~/install
# NOTE: There are additional settings under the troublehsooting section.
# bash install
# # # # # # # # # # # # # # #

# Options
# # # # # # # # # # # # # # #
# USER="${1:francis}"
LOGFILE='workstation-installation.log'

# Functions
# # # # # # # # # # # # # # #
function log {
  if [ -f "$LOGFILE" ]; then
    echo $1 >>$LOGFILE
  else
    echo $1 >$LOGFILE
  fi
}

function install {
  arr=("$@")
  _type=
  if [[ "$(declare -p arr)" =~ "declare -a" ]]; then
    for package in "${arr[@]}"; do
      if [[ $package == "package" ]]; then
        _type=$package
        continue
      fi
      if [[ $package == "flatpak" ]]; then
        _type=$package
        continue
      fi
      cnt=3
      pacname=$(echo $package | sed 's/^[^/]*\///')
      while [ $cnt -gt 0 ]; do
        if pacman -Qqe | grep $pacname; then
          break
        else
          if [[ $_type == "flatpak" ]]; then
            flatpak install flathub --noninteractive -y $package
            break
          else
            yay -S --noconfirm $package
          fi
        fi
        let "cnt-=1"
      done
      if ! pacman -Qqe | grep $pacname && [[ $_type != "flatpak" ]]; then
        log "There was a problem installing ${package}.  Please make sure this package exists."
      fi
    done
  else
    log "Warning! Passed variable is not an array".
  fi
}

log "installing commandline tools"
yay --noconfirm
install package extra/ranger
install package extra/kitty
install package aur/insync
install package extra/tmux
install package extra/virtualbox
install pacakge extra/audacity
install package omarchy/lmstudio
install pacakge extra/virtualbox-host-modules-arch
install package extra/telegram-desktop
install package extra/podman extra/buildah
install package extra/krita-plugin-gmic
install package extra/rsync
install package extra/lsof extra/strace
install package extra/gdb extra/ltrace
install package extra/htop extra/tree
install package extra/bind extra/openbsd-netcat extra/traceroute
install package extra/pdfarranger
install package extra/nvidia extra/nvidia-settings extra/cuda
install package multilib/lib32-mesa multilib/lib32-vulkan-intel multilib/lib32-vulkan-radeon multilib/lib32-nvidia-utils
install package extra/gst-plugins-good extra/gst-plugins-bad extra/gst-plugins-ugly extra/gst-libav
install package extra/wine-staging multilib/winetricks
install package extra/git-lfs
install package extra/fs-uae
install package extra/tesseract extra/tesseract-data-eng
install package aur/inform7-ide aur/lectorte-bin
install package aur/hplip-plugin
install package aur/ghostty-git
install package extra/ttf-cascadia-code-nerd
install package extra/zoxide
install package extra/zsh
install package aur/google-chrome
install package aur/python-azure-cognitiveservices-speech
install package extra/wtype extra/wl-clipboard extra/libnotify
install package aur/voxtype-bin

# interactive fiction tools
# install package aur/inform7-ide-git
# install package aur/lectrote-bin

# codecs
install package extra/libaacs
install package extra/cdrtools extra/cdrdao extra/dvd+rw-tools

# syncterm
# NOTE: Had some issues with aur version
# thanks to MeaTLoTioN for this alternative script
# https://www.erb.pw/how-to-install-syncterm-for-linux-from-source/
mkdir -p ~/lab
cd ~/lab
curl -s https://erb.pw/get-syncterm.sh | bash

# Installing configuration
# # # # # # # # # # # # # # #
log "installing custom configuration"
bash -c "$(curl -fsSL https://raw.githubusercontent.com/odysseyalive/dotfiles/master/setup)"
bash ~/.yadrlite/setup omarchy

# Enable SSH agent for passphrase caching
log "configuring ssh-agent"
systemctl --user enable --now ssh-agent.socket
if ! grep -q 'SSH_AUTH_SOCK' ~/.zshrc 2>/dev/null; then
  echo '' >> ~/.zshrc
  echo '# SSH agent socket' >> ~/.zshrc
  echo 'export SSH_AUTH_SOCK="$XDG_RUNTIME_DIR/ssh-agent.socket"' >> ~/.zshrc
fi
mkdir -p ~/.ssh && chmod 700 ~/.ssh
touch ~/.ssh/config && chmod 600 ~/.ssh/config
if ! grep -q 'AddKeysToAgent' ~/.ssh/config 2>/dev/null; then
  sed -i '1i AddKeysToAgent yes\n' ~/.ssh/config
fi

# Configure Starship prompt
log "configuring starship prompt"
mkdir -p ~/.config
cat >~/.config/starship.toml <<'EOF'
add_newline = true
command_timeout = 500
format = """
$directory
$username$time$git_branch$git_status$character"""

[character]
error_symbol = "[✗](bold red)"
success_symbol = "[❯](bold green)"

[directory]
truncation_length = 100
truncate_to_repo = false
style = "bold blue"
format = "[$path]($style)[$read_only]($read_only_style)"

[git_branch]
format = "[$symbol$branch]($style) "
style = "italic magenta"
symbol = ""

[git_status]
format = '[$all_status]($style)'
style = "yellow"
ahead = "⇡${count} "
diverged = "⇕⇡${ahead_count}⇣${behind_count} "
behind = "⇣${count} "
conflicted = " "
up_to_date = " "
untracked = "? "
modified = " "
stashed = ""
staged = ""
renamed = ""
deleted = ""

[username]
format = "[$user]($style) "
style_user = "bold green"
show_always = true

[time]
disabled = false
format = "[$time]($style) "
style = "bold cyan"
time_format = "%T"
EOF

# Configure Hyprland keyboard options.
# Omarchy 4 migrated Hyprland config from input.conf to Lua (input.lua); older
# releases used input.conf. Swap Caps Lock/Escape and enable the Chinuk Wawa
# layout toggle (Alt+Shift). The custom layout FILE itself is installed by the
# chinuk-wawa-custom-keyboard repo into ~/.config/xkb/symbols/custom.
log "configuring hyprland keyboard settings"
if [ -f ~/.config/hypr/input.lua ]; then
  if ! grep -qF "yadrlite: keyboard overrides" ~/.config/hypr/input.lua; then
    cat >>~/.config/hypr/input.lua <<'LUA'

-- yadrlite: keyboard overrides
-- Swap Caps Lock/Escape and toggle the Chinuk Wawa layout with Alt+Shift.
-- Only kb_layout/kb_options are set, so Omarchy's other input defaults remain.
hl.config({
  input = {
    kb_layout = "us,custom",
    kb_options = "caps:swapescape,grp:alt_shift_toggle",
  },
})
LUA
    log "keyboard overrides added to input.lua"
  else
    log "keyboard overrides already present in input.lua"
  fi
elif [ -f ~/.config/hypr/input.conf ]; then
  # Legacy pre-Omarchy-4 config.
  sed -i 's/^[[:space:]]*kb_options[[:space:]]*=.*/  kb_options = caps:swapescape,grp:alt_shift_toggle/' ~/.config/hypr/input.conf
fi
if pgrep -x "Hyprland" >/dev/null; then
  hyprctl reload
  log "hyprland configuration reloaded"
fi

# Nvidia environment variables.
# Omarchy 4 detects the GPU and sets NVD_BACKEND, LIBVA_DRIVER_NAME, and
# __GLX_VENDOR_LIBRARY_NAME automatically via its default/hypr/nvidia.lua, so
# the old manual injection into hyprland.conf is no longer needed (and that file
# is inert under the Lua config anyway). If you need an extra variable Omarchy
# does not set (e.g. GBM_BACKEND), add it with hl.env() in ~/.config/hypr, e.g.
# a line `hl.env("GBM_BACKEND", "nvidia-drm")` in monitors.lua or a required
# personal Lua file.
log "nvidia environment variables handled by Omarchy 4 (nvidia.lua)"

# improve sound: high sample-rate support + best-quality resampling + headroom
# for the >100% software mixer. 48kHz default with adaptive switching up to
# 192kHz; resample.quality 14 is the SoX resampler's top setting. Written with a
# heredoc (not >>) so re-running the installer replaces rather than duplicates.
mkdir -p ~/.config/pipewire/pipewire.conf.d
cat >~/.config/pipewire/pipewire.conf.d/10-rates.conf <<'EOF'
context.properties = {
  default.clock.rate = 48000
  default.clock.allowed-rates = [ 44100 48000 88200 96000 176400 192000 ]
}
stream.properties = {
  resample.quality = 14
  channelmix.max-volume = 2.0
}
EOF

# Disable audio suspend-on-idle (works around a PipeWire bug where the card
# returns from suspend with wrong/no audio). WirePlumber 0.5 removed the Lua
# config API, so this uses the SPA-JSON rules format under wireplumber.conf.d
# (the old main.lua.d/*.lua is silently ignored on 0.5+).
mkdir -p ~/.config/wireplumber/wireplumber.conf.d
cat >~/.config/wireplumber/wireplumber.conf.d/50-disable-suspend.conf <<'EOF'
monitor.alsa.rules = [
  {
    matches = [
      { node.name = "~alsa_output.*" }
      { node.name = "~alsa_input.*" }
    ]
    actions = {
      update-props = {
        session.suspend-timeout-seconds = 0
      }
    }
  }
]
EOF
# Also enable the ALSA software mixer so volume can exceed 100% (over-amp).
cat >~/.config/wireplumber/wireplumber.conf.d/50-volume-limit.conf <<'EOF'
# Enable software volume mixing on ALSA sinks for over-amplification (>100%)
monitor.alsa.rules = [
  {
    matches = [
      { node.name = "~alsa_output.*" }
    ]
    actions = {
      update-props = {
        api.alsa.soft-mixer = true
      }
    }
  }
]
EOF
systemctl --user restart wireplumber

# Note: Omarchy 4 replaced swayosd with its own Quickshell OSD, so no swayosd
# config is written. The volume keys below drive Omarchy's native OSD via
# omarchy-osd (see the omarchy-volume-osd wrapper further down).

# install global volume control script (affects ALL sinks and apps)
log "installing global volume control script"
mkdir -p ~/.local/bin
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [ -f "$SCRIPT_DIR/workstation/scripts/omarchy-volume" ]; then
  cp "$SCRIPT_DIR/workstation/scripts/omarchy-volume" ~/.local/bin/omarchy-volume
elif [ -f ~/.yadrlite/workstation/scripts/omarchy-volume ]; then
  cp ~/.yadrlite/workstation/scripts/omarchy-volume ~/.local/bin/omarchy-volume
else
  # Create the script inline if not found
  cat >~/.local/bin/omarchy-volume <<'VOLSCRIPT'
#!/bin/bash
# Global volume control - affects ALL sinks and streams simultaneously

ACTION="$1"
AMOUNT="${2:-5}"
MAX_VOLUME="${3:-200}"

get_all_sinks() {
    pactl list short sinks | awk '{print $1}'
}

get_all_sink_inputs() {
    pactl list short sink-inputs | awk '{print $1}'
}

get_current_volume() {
    pactl list sinks | grep -E "^\s+Volume:" | head -1 | grep -oP '\d+%' | head -1 | tr -d '%'
}

set_volume_all() {
    local vol="$1"
    if [ "$vol" -gt "$MAX_VOLUME" ]; then
        vol="$MAX_VOLUME"
    elif [ "$vol" -lt 0 ]; then
        vol=0
    fi
    for sink in $(get_all_sinks); do
        pactl set-sink-volume "$sink" "${vol}%"
    done
    for input in $(get_all_sink_inputs); do
        pactl set-sink-input-volume "$input" "${vol}%"
    done
    echo "$vol"
}

case "$ACTION" in
    raise|up|+)
        current=$(get_current_volume)
        new=$((current + AMOUNT))
        set_volume_all "$new"
        ;;
    lower|down|-)
        current=$(get_current_volume)
        new=$((current - AMOUNT))
        set_volume_all "$new"
        ;;
    mute-toggle|toggle)
        for sink in $(get_all_sinks); do
            pactl set-sink-mute "$sink" toggle
        done
        for input in $(get_all_sink_inputs); do
            pactl set-sink-input-mute "$input" toggle
        done
        ;;
    set)
        set_volume_all "$AMOUNT"
        ;;
    get)
        get_current_volume
        ;;
    balance)
        for sink in $(get_all_sinks); do
            vol=$(pactl get-sink-volume "$sink" | grep -oP '\d+%' | head -1 | tr -d '%')
            pactl set-sink-volume "$sink" "${vol}%" 2>/dev/null
        done
        for input in $(get_all_sink_inputs); do
            vol=$(pactl get-sink-input-volume "$input" 2>/dev/null | grep -oP '\d+%' | head -1 | tr -d '%')
            [ -n "$vol" ] && pactl set-sink-input-volume "$input" "${vol}%" 2>/dev/null
        done
        echo "Balance fixed"
        ;;
    *)
        echo "Usage: omarchy-volume <raise|lower|mute-toggle|set|get|balance> [amount] [max-volume]"
        exit 1
        ;;
esac
VOLSCRIPT
fi
chmod +x ~/.local/bin/omarchy-volume
log "global volume control script installed"

# Volume keys: 200% max across ALL sinks/apps. Omarchy 4's native
# omarchy-audio-output-volume clamps at 100% and only touches the resolved sink,
# so we keep the custom all-sink script and override the volume keys. A tiny
# wrapper pairs the volume change with Omarchy's native OSD (omarchy-osd), which
# also keeps the Hyprland binding free of nested shell quoting under Lua.
log "installing 200% volume OSD wrapper"
cat >~/.local/bin/omarchy-volume-osd <<'OSD'
#!/bin/bash
# Adjust volume across ALL sinks/apps (200% max) and show the Omarchy native OSD.
# Usage: omarchy-volume-osd <raise|lower> [step]
dir="$1"
step="${2:-3}"

vol="$(~/.local/bin/omarchy-volume "$dir" "$step" 200 2>/dev/null | tail -1)"
[[ "$vol" =~ ^[0-9]+$ ]] || vol="$(~/.local/bin/omarchy-volume get 2>/dev/null | tail -1)"
vol="${vol:-0}"

if ((vol == 0)); then
  icon="volume-muted"
else
  icon="volume-high"
fi

omarchy-osd -i "$icon" -p "$vol"
OSD
chmod +x ~/.local/bin/omarchy-volume-osd

# Bind the volume keys in the Lua config (Omarchy 4), falling back to
# bindings.conf on older setups.
if [ -f ~/.config/hypr/bindings.lua ]; then
  if ! grep -qF "yadrlite: 200% volume" ~/.config/hypr/bindings.lua; then
    cat >>~/.config/hypr/bindings.lua <<'LUA'

-- yadrlite: 200% volume — all sinks/apps, overriding Omarchy's 100%-capped keys.
hl.unbind("XF86AudioRaiseVolume")
hl.unbind("XF86AudioLowerVolume")
hl.unbind("ALT + XF86AudioRaiseVolume")
hl.unbind("ALT + XF86AudioLowerVolume")
o.bind("XF86AudioRaiseVolume", "Volume up", "omarchy-volume-osd raise 3", { locked = true, repeating = true })
o.bind("XF86AudioLowerVolume", "Volume down", "omarchy-volume-osd lower 3", { locked = true, repeating = true })
o.bind("ALT + XF86AudioRaiseVolume", "Volume up precise", "omarchy-volume-osd raise 1", { locked = true, repeating = true })
o.bind("ALT + XF86AudioLowerVolume", "Volume down precise", "omarchy-volume-osd lower 1", { locked = true, repeating = true })
LUA
    log "200% volume key bindings added to bindings.lua"
  fi
elif [ -f ~/.config/hypr/bindings.conf ]; then
  if ! grep -q "omarchy-volume" ~/.config/hypr/bindings.conf; then
    cat >>~/.config/hypr/bindings.conf <<'EOF'

# Volume keys with 200% max volume support - controls ALL sinks/apps globally
$osdclient = swayosd-client --monitor "$(hyprctl monitors -j | jq -r '.[] | select(.focused == true).name')"
bindeld = ,XF86AudioRaiseVolume, Volume up, exec, ~/.local/bin/omarchy-volume raise 3 200 && $osdclient --output-volume raise --max-volume 200
bindeld = ,XF86AudioLowerVolume, Volume down, exec, ~/.local/bin/omarchy-volume lower 3 200 && $osdclient --output-volume lower --max-volume 200
bindeld = ALT, XF86AudioRaiseVolume, Volume up precise, exec, ~/.local/bin/omarchy-volume raise 1 200 && $osdclient --output-volume +1 --max-volume 200
bindeld = ALT, XF86AudioLowerVolume, Volume down precise, exec, ~/.local/bin/omarchy-volume lower 1 200 && $osdclient --output-volume -1 --max-volume 200
EOF
    log "hyprland volume bindings configured for global volume control with 200% max"
  fi
fi

# Fix Lenovo dock USB autosuspend loop (VIA Labs USB 3.1 hub)
# Prevents kernel from repeatedly trying to enumerate/suspend dock ports
log "configuring lenovo dock usb fix"
if ! [ -f /etc/udev/rules.d/50-lenovo-dock.rules ]; then
  echo '# Disable autosuspend for Lenovo dock USB hub (VIA Labs)
ACTION=="add", SUBSYSTEM=="usb", ATTR{idVendor}=="17ef", ATTR{idProduct}=="307f", ATTR{power/control}="on"' | sudo tee /etc/udev/rules.d/50-lenovo-dock.rules >/dev/null
  sudo udevadm control --reload-rules
  log "lenovo dock usb udev rule installed"
fi

# Fix Realtek RTS522A card reader autosuspend (microSD not detected)
# Kernel power management suspends the PCI card reader, preventing card detection
log "configuring card reader power fix"
if ! [ -f /etc/udev/rules.d/50-realtek-card-reader.rules ]; then
  echo '# Disable autosuspend for Realtek RTS522A PCI-E card reader
ACTION=="add", SUBSYSTEM=="pci", ATTR{vendor}=="0x10ec", ATTR{device}=="0x522a", ATTR{power/control}="on"' | sudo tee /etc/udev/rules.d/50-realtek-card-reader.rules >/dev/null
  sudo udevadm control --reload-rules
  log "realtek card reader udev rule installed"
fi

# clean everything
yay -Scc --noconfirm

log "Installation complete.  Please reboot your system."
printf "\r# # # # # # # # # # # # # # # # # # # # # # # # # # # # # #\r"
printf "ALL DONE\r"
printf "Review the workstation-installation.login and address any uninstalled packages noted.\r"
printf "PLEASE REBOOT YOUR SYSTEM."
